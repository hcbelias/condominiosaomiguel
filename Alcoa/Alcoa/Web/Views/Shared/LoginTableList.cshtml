@using Util
@model IEnumerable<Model.LoginModel>
<table id="table-list" class="table table-bordered  table-hover table-striped table-condensed">
    <thead>
        <th style="width: 8%; text-align:center;">@Html.Label(Constants.Translate.ACOES)
        </th>
	<th style="text-align:center;">@Html.Label(Internationalization.Id)</th>
	<th style="text-align:center;">@Html.Label(Internationalization.Password)</th>
	<th style="text-align:center;">@Html.Label(Internationalization.Email)</th>
	</thead>
	@if(Model!=null)
	{
		<tbody>
		@foreach (var item in Model)
		{
			<tr>
			<td style="width: 8%;text-align:center;">
				@Html.Partial("LoginAction",item);
            </td>
				
				
					<td style="text-align:center;">@Html.DisplayFor(modelItem => item.Password)</td>

				
					<td style="text-align:center;">@Html.DisplayFor(modelItem => item.Email)</td>

				
			</tr>

		}
		</tbody>
	}
</table>





















   