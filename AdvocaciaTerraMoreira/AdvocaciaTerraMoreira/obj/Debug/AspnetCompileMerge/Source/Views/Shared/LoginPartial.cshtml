
@if(Request.IsAuthenticated) {
        
    @Html.Raw("Sair:  ")
    @Html.ActionLink(User.Identity.Name, "Logout", "Login")

}
else
{
    @Html.ActionLink("Log in", "Login", "Login", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
